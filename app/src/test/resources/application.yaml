spring:
  datasource:
    url: jdbc:h2:mem:attestry;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration-test

app:
  auth:
    token:
      provider: MEMORY
  storage:
    provider: MEMORY
    bucket: test-onboarding
  user-auth:
    signup-email-verification:
      mail:
        provider: LOG
        enabled: false
      fixed-code: 12345678
    invitation:
      mail:
        provider: LOG
        enabled: false
  product:
    public-base-url: http://localhost:8080
  kafka:
    enabled: false
